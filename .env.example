# Local dev only. This file is gitignored.
# Set a strong SA password (meets SQL Server complexity rules).
ACCEPT_EULA=Y            # Microsoft: set to accept the SQL Server image EULA (required for the image)
MSSQL_PID=Developer      # Microsoft: SQL Server edition/product key (e.g., Developer, Express, Evaluation, EnterpriseCore, ...)
MSSQL_DB=ra              # Repo-specific: DB name our init/bootstrap uses (NOT a built-in SQL Server env var)
MSSQL_SA_PASSWORD=Its-5foutside     # Microsoft: sets the 'sa' password (must meet SQL Server password policy)
